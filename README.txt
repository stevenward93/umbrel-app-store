This v2 zip adds the fields the official Umbrel community-store template expects
in umbrel-app.yml, including manifestVersion, gallery, releaseNotes,
dependencies, path, defaultUsername, and defaultPassword.

Upload the CONTENTS of this zip to the ROOT of your GitHub repo.
